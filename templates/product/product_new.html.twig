{# templates/product/product_new.html.twig #}

{# Rôle>
    Sert le formulaire d'ajout d'un produit
Rôle #}

{% extends "base.html.twig" %}

{% block body %}

    {% if product is defined %}
        <h1>Formulaire de modification d'un produit</h1><br>
    {% else %}
        <h1>Formulaire de création d'un produit</h1><br>
    {% endif %}

     {{
     form(form, {'attr': {novalidate:'novalidate'}})
     }}

    {% if product is defined %}

        <a class="Button -danger" href="{{ path('manage_product_delete_confirm', {'id':product.id}) }}">Supprimer</a>

    {% endif %}

    {# {{ form_start(form, {'attr': {novalidate:'novalidate'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }} #}

{% endblock %}